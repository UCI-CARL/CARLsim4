#!/bin/bash

numNeurons='1000 3160'

for numN in $numNeurons # number of neurons, which defines the workload
do
	for iter in {1..20} # 20 iterations
	do
		echo $numN $iter
		./benchmark $numN $iter 0 s.csv # single GPU
		./benchmark $numN $iter 1 m.csv # multi GPUs
	done
done


